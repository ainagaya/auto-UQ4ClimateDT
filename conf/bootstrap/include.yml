DEFAULT:
  CUSTOM_CONFIG:
    PRE:
      - "%PROJDIR%/conf/platforms.yml"
      - "%PROJDIR%/conf/general.yml"
      - "%PROJDIR%/conf/models/%MODEL.NAME%.yml"
      - "%PROJDIR%/conf/ics/%MODEL.ICS%.yml"
      - "%PROJDIR%/conf/simulation/%MODEL.SIMULATION%.yml"
